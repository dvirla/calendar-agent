# Calendar Agent Frontend Environment Variables

# Backend API URL
# For development, this should point to your local backend server
# For production, this should point to your deployed backend
VITE_API_BASE_URL=http://localhost:8000

# Optional: Custom app title (defaults to Calendar Agent)
VITE_APP_TITLE=Calendar Agent

# Optional: Environment identifier for debugging
VITE_APP_ENV=development

# Note: This frontend requires a compatible backend API to function
# See backend_README.md for backend setup instructions
# 
# The backend must provide the following endpoints:
# - /auth/google (OAuth initiation)
# - /auth/callback (OAuth callback) 
# - /user/profile (User profile)
# - /chat (AI chat)
# - /calendar/events (Calendar operations)
# - /actions/pending (Pending actions)
# - /actions/approve/{id} (Approve actions)
# - /actions/reject/{id} (Reject actions)